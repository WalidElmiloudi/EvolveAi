<IfModule mod_rewrite.c>

  Options -Multiviews
  
  RewriteEngine On
  RewriteBase /EvolveAi/public

  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  RewriteRule ^(.+)$ index.php?url=$1 [QSA,L]

</IfModule>